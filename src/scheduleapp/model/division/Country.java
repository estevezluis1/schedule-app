package scheduleapp.model.division;

/**
 * Data model for country, extends Division.
 */
public class Country extends Division {

    /**
     * Extends Division.
     * @param id of country.
     * @param name of country.
     */
    public Country(int id, String name) {
        super(id, name);
    }
}
